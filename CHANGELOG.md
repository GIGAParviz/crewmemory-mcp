# Changelog

## 0.3.0 - 2026-08-26

- Add per-session project and branch selection through `team_context(project_path=...)`.
- Persist connection settings so `doctor` and the dashboard work outside an MCP client.
- Add portable `uvx` launcher configuration for one-command installs after publication.
- Add protocol-level MCP, packaging, installer, Git sync, and dashboard tests.

## 0.2.0

- Add the human dashboard, lifecycle tools, project-aware retrieval, and multi-client installer.
